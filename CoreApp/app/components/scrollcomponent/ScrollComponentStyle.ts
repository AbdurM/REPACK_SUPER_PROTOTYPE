// generated from template

import {StyleSheet} from 'react-native';
import { UI } from 'utilities/Helpers';

export const ScrollComponentStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: UI.responsiveWidth(2),
  },
  loadercontainer:{
    flex:1, 
    justifyContent:'center',
    alignItems:'center',
  },
  lazyLoaderContainer:{
    height: UI.responsiveWidth(8),
  }
});
